data:
  candidate_rows_csv: outputs/hybrid_routing_dataset/candidate_rows.csv

output_dir: outputs/hybrid_strategy_recommender/real_math500

split:
  seed: 42

features:
  families:
    - prompt_features
    - action_features
    - interaction_features
    - heuristic_features

heuristics:
  enabled: true
  rules:
    - prune_dominated_cost
    - cheap_safe
    - revise_favorable
    - answer_format_risk

model:
  type: logistic
  calibration: sigmoid
  params:
    max_iter: 2000

utility:
  name: expected_correct_minus_lambda_cost
  lambda: 1.1
  beta_uncertainty: 0.05

optimizer:
  type: mckp_exact
  params:
    cost_scale: 100

budget:
  avg_budget_per_prompt: 1.25

ablation:
  run_all: false

